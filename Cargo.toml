[package]
name = "kleio-cli_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
kleio = { path = "../kleio", features = ["ged-io"] }

[[bin]]
name = "kleio-cli"
path = "src/main.rs"
